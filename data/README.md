# Local runtime data

This directory is reserved for local development data.

Never commit generated JSON stores, PDFs, logos, customer details, company
details, or other production data. Keep only anonymized fixtures inside the
test directories.
